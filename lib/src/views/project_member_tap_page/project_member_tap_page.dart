import 'package:flutter/material.dart';

class ProjectMemberTapPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('달력')),
      body: Center(child: Text('ProjectMemberTapPage')),
    );
  }
}
